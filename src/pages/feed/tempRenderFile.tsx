/*
 * TEMPORARY FILE - FOR DEMO PURPOSES ONLY
 * This file is temporary and will be deleted once the draft is accepted.
 */

import FeedPage from "./index";

/**
 * Render wrapper component for FeedPage
 * 
 * Demonstrates the FeedPage component which includes:
 * - Avatar component with user profile information
 * - Follow button functionality
 * - FeedGrid component displaying feed cards in a grid layout
 */
function FeedPageRender() {
  return <FeedPage />;
}

export default FeedPageRender;
